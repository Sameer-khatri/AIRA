export function buildMockHomeReply(userText: string) {
  return `I understand. You’re asking about “${userText}”.\n\nFor now, this is a simulated response from the Personal Home lab. When the real conversation service is connected, my complete answer will appear here and remain available.`;
}
