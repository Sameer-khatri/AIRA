[project]
name = "aira-backend"
version = "0.1.0"
description = "AIRA local backend engine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlmodel",
    "python-dotenv",
    "ollama"
]

[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"
